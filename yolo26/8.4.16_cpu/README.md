# Yolo26

[Yolo26](https://docs.ultralytics.com/models/yolo26/), 
uses PyTorch 2.x and Ultralytics 8.4.16

## Quick start

### Directories

Create the following directories where you want to start the Docker image from:

```
mkdir cache
mkdir config
```

### Inhouse registry

* Pull and run image (adjust volume mappings `-v`):

  ```bash
  docker run --shm-size 8G --net=host \
    -v `pwd`:/workspace \
    -v `pwd`/cache:/cache \
    -v `pwd`/config:/config \
    -it harbor.cms.waikato.ac.nz/public/pytorch/yolo26:8.4.16_cpu
  ```

### Docker hub

* Pull and run image (adjust volume mappings `-v`):

  ```bash
  docker run --shm-size 8G --net=host \
    -v `pwd`:/workspace \
    -v `pwd`/cache:/cache \
    -v `pwd`/config:/config \
    -it waikatodatamining/pytorch-yolo26:8.4.16_cpu
  ```

### Build local image

* Build the image from Docker file (from within /path_to/yolo26/8.4.16_cpu)

  ```bash
  docker build -t yolo26 .
  ```
  
* Run the container

  ```bash
  docker run --shm-size 8G --net=host \
    -v `pwd`:/workspace \
    -v `pwd`/cache:/cache \
    -v `pwd`/config:/config \
    -it yolo26
  ```
  `/local/dir:/container/dir` maps a local disk directory into a directory inside the container


## Publish images

### Build

```bash
docker build -t yolo26:8.4.16_cpu .
```

### Inhouse registry  

* Tag

  ```bash
  docker tag \
    yolo26:8.4.16_cpu \
    harbor.cms.waikato.ac.nz/public/pytorch/yolo26:8.4.16_cpu
  ```
  
* Push

  ```bash
  docker push harbor.cms.waikato.ac.nz/public/pytorch/yolo26:8.4.16_cpu
  ```
  If error "no basic auth credentials" occurs, then run (enter username/password when prompted):
  
  ```bash
  docker login harbor.cms.waikato.ac.nz
  ```

### Docker hub  

* Tag

  ```bash
  docker tag \
    yolo26:8.4.16_cpu \
    waikatodatamining/pytorch-yolo26:8.4.16_cpu
  ```
  
* Push

  ```bash
  docker push waikatodatamining/pytorch-yolo26:8.4.16_cpu
  ```
  If error "no basic auth credentials" occurs, then run (enter username/password when prompted):
  
  ```bash
  docker login
  ``` 


### Requirements

```bash
docker run --rm --pull=always \
  -it harbor.cms.waikato.ac.nz/public/pytorch/yolo26:8.4.16_cpu \
  pip freeze > requirements.txt
```


## Permissions

When running the docker container as regular use, you will want to set the correct
user and group on the files generated by the container (aka the user:group launching
the container):

```bash
docker run -u $(id -u):$(id -g) -e USER=$USER ...
```


## Scripts

The following additional scripts are available:

* `yolo26_train` - the yolo26 script for training models
* `yolo26_export` - the yolo26 script for exporting models (eg to ONNX)
* `yolo26_predict_poll` - batch-processing of images via file-polling
* `yolo26_predict_redis` - making predictions via Redis backend


### yolo26_predict_redis
 
You need to start the docker container with the `--net=host` option if you are using the host's Redis server.


### yoloe text prompts

Unfortunately, clip downloads its models to the current working directory.
The image sets this to /tmp to make this work out of the box, but it will
download the model every time a container is launched. You can avoid this
by providing a specific working directory on the command-line, e.g.:

```bash
  --workdir /workspace \
```
